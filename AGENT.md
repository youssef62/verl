
- Read `DESIGN.md` to understand what we're building.
- Read `progress.md` to understand what we've done so far.
- Every validated change should be documented in `progress.md`. 
- To check if you change is valid, run `sbatch multi_tenant_run.sh` and check the results in `logs/`.
